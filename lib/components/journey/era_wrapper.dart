import 'package:safeandromeda/core/hooks/hooks.dart';

class EraWrapper extends StatelessWidget {
  const EraWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
