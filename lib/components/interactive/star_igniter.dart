import 'package:safeandromeda/core/hooks/hooks.dart';

class StarIgniter extends StatelessWidget {
  const StarIgniter({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox();
  }
}
